<% if (errors.length > 0) { %>
<div class="bg-red-100 text-red-500 p-4 rounded-md">
  <% errors.forEach(error => { %> <%= error %><br />
  <% }) %>
</div>
<% } %> <% if (success.length > 0) { %>
<div class="bg-green-100 text-green-500 p-4 rounded-md">
  <% success.forEach(smg => { %> <%= smg %><br />
  <% }) %>
</div>
<% } %>
